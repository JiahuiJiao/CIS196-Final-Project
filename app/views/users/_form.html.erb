<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, class: "form_label" %><br>
    <%= f.text_field :name, class: "form_field" %>
  </div>
  <div class="field">
    <%= f.label :password, class: "form_label" %><br>
    <%= f.password_field :password, class: "form_field" %>
  </div>
  <div class="field">
    <%= f.label :email, class: "form_label" %><br>
    <%= f.text_field :email, class: "form_field" %>
  </div>
  <div class="actions">
    <%= f.submit "Create User", :class => "form_button" %>
  </div>
<% end %>
